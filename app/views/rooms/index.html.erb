<div id='chatroom_container' class="container d-flex pt-4">
  <div id="side_panel" class="col-md-4 col-sm-4 me-3 mb-3">
    <h5> Hi <%= @current_user.email %> </h5>
    <hr>
    <h4> Users </h4>
    <%= turbo_stream_from 'users' %>
    <div id="users">
      <%= render @users %>
    </div>
    <hr>
    <h4> Rooms </h4>
    <%= render 'rooms/new_room_form' %>
    <%= turbo_stream_from 'rooms' %>
    <div id="rooms">
      <%= render @rooms %>
    </div>
  </div>
  <div id="room_container" class="w-100">
    <% if @single_room %>
      <div id="single_room" class="shadow p-3 mb-5 bg-body rounded">
        <h4 class="text-center"><%= @user&.email || @single_room.name %></h4>
        <hr>
        <%= turbo_stream_from @single_room %>
        <%= render 'messages/new_message_form' %>
      </div>
    <% end  %>
  </div>

</div>
